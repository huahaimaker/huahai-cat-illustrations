# 德文猫 Prompt 示例库

下面这些指令可以直接复制到 Codex 中使用。把占位内容替换成你的文章、文件路径、图片或观点即可。

## 只做配图规划

```text
Use $huahai-cat-illustrations 先不要生图。
请分析下面这篇文章哪里真正值得配图，输出 5 张左右的 shot list。
每张写清楚：
- 放在哪个段落后
- 图的主题
- 唯一核心意思
- 物理隐喻与构图类型
- 德文猫在图里做什么
- 主要物件
- 建议中文短标注

<粘贴文章>
```

## 直接生成文章正文配图

```text
Use $huahai-cat-illustrations 为下面这篇中文文章设计并生成 4 张德文猫正文配图。
要求：16:9 横版、白底彩色手绘、大量留白、一图一事。
德文猫必须承担改变结果的关键动作，不要做成角色海报或 PPT 信息图。

<粘贴文章>
```

## 生成 3 组小红书封面

```text
Use $huahai-cat-illustrations 为下面 3 个选题分别设计并生成小红书封面。
固定尺寸：3:4 竖屏，1080 × 1440 px，每张单独输出。

版式分别使用：判断型、情绪共鸣型、教程型。
角色保持同一只德文猫，但三张必须使用三套明显不同的完整造型：
1. 签名绿色夹克
2. 角色库中的居家/情绪造型
3. 角色库中的蓝色工作学习造型

标题必须逐字正确；四边保留约 8% 安全区；不要拼成宫格。

选题：
1. <标题一>
2. <标题二>
3. <标题三>
```

## 生成公众号正文配图

```text
Use $huahai-cat-illustrations 为下面这篇公众号文章设计并生成 3 张正文配图。
固定尺寸：1080 × 608 px、横版；不放海报式大标题，每张只保留 0–3 个必要短标注。

三张分别表达：认知闭环、状态拆解、行动变化。
角色保持同一德文猫身份，但至少轮换两套完整造型，不要每张都穿绿色夹克。

<粘贴文章>
```

## 生成公众号首条头图

```text
Use $huahai-cat-illustrations 为这篇文章生成一张公众号首条头图。
尺寸 900 × 383 px、2.35:1；核心脸部和标题关键词放在中央约 383 × 383 px 安全区。
不要把已有 16:9 正文配图直接拉伸。

标题：<不超过 12 个汉字>
文章核心：<一句话>
```

## 长文配图策略

```text
Use $huahai-cat-illustrations 给这篇长文做配图策略。
不要按段落平均配图，只挑认知锚点：核心判断、输入输出、关键转折、常见误区、验证动作和承接路径。
规划 6–8 张，先输出 shot list，不生成图片。

<粘贴文章>
```

## 为单个观点生成一张图

```text
Use $huahai-cat-illustrations 为这个观点生成一张 16:9 正文配图：

“信任不是喊出来的，而是一块证据一块证据拼出来的。”

请重新发明一个低科技物理隐喻，让德文猫亲手完成关键动作。
中文标注最多 5 处，每处尽量不超过 6 个字。
```

## 从案例库选一个方向再原创

```text
Use $huahai-cat-illustrations 参考 examples/case-library.md 中“选题雷达台”的思考方式，
为下面这段内容重新设计一张图。只借鉴“如何把抽象判断变成动作”，不要复刻雷达、构图或标注。

<粘贴内容>
```

## 生成并插入 Obsidian / Markdown

```text
Use $huahai-cat-illustrations 为这个 Markdown 文件生成 4 张正文配图。
把图片保存到 assets/<article-slug>-devon-cat/，按 01-topic-name.png 顺序命名。
实际检查图片后，把相对 Markdown 图片链接插入对应认知锚点后，不要改动文章其他文字。

文件：<绝对路径>
```

## 将旧角色插画重新设计为德文猫

```text
Use $huahai-cat-illustrations 重新设计这张旧角色正文配图。
保留它想表达的核心观点，但不要贴图换脸，也不要照抄旧构图。
请让德文猫成为真正推动结构运转的人，并保持白底彩色手绘风格。

图片：<图片路径>
```

## 修正角色漂移

```text
Use $huahai-cat-illustrations 编辑这张图。
保持核心构图和动作，只根据内置角色三视图修正德文猫：奶白毛色、大尖耳、蓝眼、绿色米白夹克、卡其工装裤、绿白鞋、黑色斜挎包和一条卷尾。
删除多余肢体、重复尾巴和参考图背景污染。
```

## 去掉错误文字或多余标题

```text
Use $huahai-cat-illustrations 编辑这张图。
只删除左上角的“流程图”标题和错误标注“有效信号号”，用相同白色背景自然补齐。
其他角色、物件、标注、构图和比例保持不变，不要新增内容。
```

## 生成一组风格样片

```text
Use $huahai-cat-illustrations 生成 5 张不同主题的德文猫正文配图样片。
主题分别覆盖：信息过载、用户验证、内容复利、人机协作、信任建立。
每张单独生成，不要拼图；构图和主要物件不能重复。
先写造型分配表，至少使用 3 套角色库中的完整造型；签名绿色夹克不超过 2 张。
```

## 单角色基础手势

```text
Use $huahai-cat-illustrations 生成一张德文猫挥手欢迎的全身 IP 素材。
使用三视图作为身份主参考、基础手势库作为副参考。
保持签名绿色夹克造型，4:5、纯白背景、只出现一个角色，不要文字和其他姿势。
```

## 一组独立情绪素材

```text
Use $huahai-cat-illustrations 生成 6 张德文猫情绪素材：开心、惊讶、困惑、焦虑、难过、自信。
参考内置情绪谱系库，每个表情单独调用和保存，统一身份与线条，不要拼成九宫格。
只有难过场景可以短暂垂耳，其他表情保持大耳直立。
```

## 四季生活场景

```text
Use $huahai-cat-illustrations 生成一张“雨天也可以慢慢前进”的德文猫生活场景。
使用三视图和四季生活库中的雨衣雨靴造型，只借鉴一套完整穿搭与雨伞动作。
16:9 白底，场景克制，不要复制八宫格或加入其他季节服装。
```

## 学习工作状态

```text
Use $huahai-cat-illustrations 为“真正的学习要经过输入、输出和验证”生成一张正文配图。
使用学习工作库作为副参考，让德文猫亲自阅读、做笔记并验证。
不要画知识库 UI，不要直接复制参考图中的矮桌构图。
```

## 运动与行动力

```text
Use $huahai-cat-illustrations 为“行动之后才产生动力”生成一张德文猫正文配图。
使用运动娱乐库中的跑步姿势和完整运动造型作为副参考。
保持一个角色、清楚动作和大量前进方向留白，不要做体育海报。
```

## 使用已确认造型改图

```text
Use $huahai-cat-illustrations 编辑这张德文猫图片。
保持角色身份、脸、动作和构图，只把服装换成四季生活库中的蓝色雨衣雨靴整套造型，并保留一把黄色雨伞。
不要混入其他参考服装、动作或八宫格布局。
```

## 完全随机生成

```text
Use $huahai-cat-illustrations 随机选择 4 个适合知识型创作者的抽象主题，
为每个主题重新发明一个低科技物理隐喻，并分别生成德文猫正文配图。
不要先问我选题，每张独立输出。
```
